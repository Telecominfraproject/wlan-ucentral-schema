description:
  This section can be used to setup NAT logging on the AP.
type: object
properties:
  enabled:
    description:
      This option defines if NAT logging shall be enabled.
    type: boolean
    default: false
